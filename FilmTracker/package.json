{
  "name": "filmtracker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "sass --watch style:style",
    "bu": "http-server -a localhost -p 8080 -c-1",
    "dev": "concurrently \"npm run watch\" \"npm run bu\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.0"
  }
}
